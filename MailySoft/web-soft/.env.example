# URL base de la API. En dev usamos ruta relativa: el proxy de Vite
# (vite.config.ts) reenvía /api → http://localhost:8000, así front y backend
# son el MISMO origen y las cookies httpOnly funcionan sin líos de CORS.
VITE_API_URL=/api/v1
